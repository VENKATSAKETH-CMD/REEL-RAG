# --- DB + JWT base config ---
DATABASE_URL=postgresql+psycopg://venkat@localhost:5432/reels


SECRET_KEY=dev-secret-key-change-me
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# --- File storage (where uploads are saved) ---
STORAGE_LOCAL_PATH=./data/uploads

# --- Phase 2: keep everything FREE (stub mode) ---
ASR_MODE=stub
EMBED_MODE=stub

